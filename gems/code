#!/bin/bash

if [[ $1 = "r" ]]; then
    sudo rm -f /usr/bin/code_default
    gem pac R code
    
    exit 0
fi

gem pac S code

sudo mv /usr/bin/code /usr/bin/code_default

printf '#!/bin/bash\n\ncode_default --ozone-platform=wayland' | sudo tee /usr/bin/code