#!/bin/bash

if [[ $1 = "r" ]]; then
    sudo sed -i "/ParallelDownloads/s/^/#/g" /etc/pacman.conf
    
    exit 0
fi

echo "[gem] Enabled ParallelDownloads in /etc/pacman.conf"
sudo sed -i "/ParallelDownloads/s/^#//g" /etc/pacman.conf